<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-feature android:name="android.hardware.camera"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:hasCode="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:networkSecurityConfig="@xml/network_security_config"
        android:localeConfig="@xml/locales_config">
        <meta-data
            android:name="android.app.category"
            android:value="android.app.category.MEDIA" />
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc"/>
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|mnc|colorMode|density|fontScale|fontWeightAdjustment|keyboard|layoutDirection|locale|mcc|navigation|smallestScreenSize|touchscreen|uiMode"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- OAuth callback: musicassistant://auth/callback?code=... -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="musicassistant"
                    android:host="auth"
                    android:path="/callback" />
            </intent-filter>
            <!-- In-app page deep links: musicassistant://app/home|library|search -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="musicassistant"
                    android:host="app" />
            </intent-filter>
            <!--
                App Links: https://(www.)music-assistant.io/app/<page>. autoVerify
                attempts Digital Asset Links verification against
                /.well-known/assetlinks.json on the host. If that file can't be
                hosted (domain not under our control) verification fails silently
                and the link is NOT auto-claimed — the user opts in manually via
                Settings → Apps → Music Assistant → Open by default → Add link.

                Scoped to exactly "/app" and the "/app/" subtree so the site's
                homepage and any other page (incl. lookalikes like /apps, /apple)
                stay with the browser. assetlinks.json can't express paths — it is
                a host-level grant — so this manifest scoping is what keeps the app
                from shadowing the rest of the site.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:host="www.music-assistant.io" />
                <data android:host="music-assistant.io" />
                <data android:path="/app" />
                <data android:pathPrefix="/app/" />
            </intent-filter>
            <!-- App Actions capability map (actions.intent.PLAY_MEDIA, etc.) -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>
        <!--
            Headless dispatch Activity for App Actions / Assistant voice playback.
            App Actions fulfilment intents can only target Activities, so this
            Theme.NoDisplay shim binds AndroidAutoPlaybackService via
            MediaBrowserCompat and invokes playFromSearch on the resulting
            MediaController — same path the in-car AA framework uses. The
            Activity finishes synchronously; the async bind is owned by a
            process-scoped singleton (see VoicePlayDispatchActivity.kt).
        -->
        <activity
            android:name=".VoicePlayDispatchActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.media.action.MEDIA_PLAY_FROM_SEARCH" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <service
            android:name="io.music_assistant.client.services.AndroidAutoPlaybackService"
            android:exported="true">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
                <category android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.media.action.MEDIA_PLAY_FROM_SEARCH" />
            </intent-filter>
            <meta-data
                android:name="android.media.browse.service"
                android:resource="@xml/automotive_app_desc" />
        </service>
        <service
            android:name=".services.MainMediaPlaybackService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <!-- Serves browse-row artwork to media hosts as opaque content:// URIs, fetched in
             this process. Not exported: readable only through the prefix grant that
             AndroidAutoPlaybackService.onGetRoot hands to a verified, media-trusted caller. -->
        <provider
            android:name=".auto.AndroidAutoArtworkProvider"
            android:authorities="${applicationId}.autoartwork"
            android:exported="false"
            android:grantUriPermissions="true" />
    </application>

</manifest>